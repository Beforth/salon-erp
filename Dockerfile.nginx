# Main app is proxied to Vite dev server (no dist). Placeholder dir for nginx.
FROM nginx:alpine
RUN rm -f /etc/nginx/conf.d/default.conf && mkdir -p /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80 443
CMD ["nginx", "-g", "daemon off;"]
